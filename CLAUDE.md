# Катя ♥ Діма — весілля-гра (лютевесілля.укр)

Це бриф для Claude, який працюватиме з цим репозиторієм далі.
Власниця проєкту — Катя (kateliuta@gmail.com). Вона **не програмістка**:
описує словами, що треба, і перевіряє результат очима. Пояснюй простою мовою,
без жаргону, і завжди кажи, що саме зміниться на екрані.

## Що це

Запрошення на весілля у вигляді 8-бітної гри. Весілля відбулося 6 вересня 2026.
Після весілля сайт лишився жити як **публічний кейс**: вхід без пароля, усе відкрито.

Живе на GitHub Pages: `git push` → за 1-2 хвилини оновлюється лютевесілля.укр.

## Золоті правила цього репозиторію

1. **index.html — один файл на 1,2 МБ і 12 500 рядків.** Ніяких фреймворків,
   збірки, npm, бекенду. Усе інлайном: CSS, JS, шрифти в base64.
   Це свідомий вибір, не технічний борг. Не пропонуй «давай розіб'ємо на модулі».
2. **Ніколи не переписуй index.html цілком і не передруковуй його вміст.**
   Правь точковими замінами: python-скрипт із `assert s.count(old)==1`, або `sed -i`.
   Якщо перевипишеш файл із пам'яті — загубиш половину гри.
3. **Після кожної правки перевіряй синтаксис:**
   ```
   node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');
   const m=h.match(/<script[^>]*>[\s\S]*?<\/script>/g)||[];
   m.forEach((b,i)=>{if(/ld\+json/.test(b.slice(0,60)))return;
   const c=b.replace(/^<script[^>]*>/,'').replace(/<\/script>$/,'');
   if(c.trim()){try{new Function(c)}catch(e){console.log('ERR',i,e.message)}}});"
   ```
4. **Катя не має мережі до GitHub із Claude.** `git push` вона робить сама в Terminal.
   Комітити можна й треба, пушити — просити її.
5. У `.git` іноді лишаються `index.lock` / `HEAD.lock` від обірваних операцій.
   Якщо git скаржиться — видали ці файли й повтори.

## Файли

| Файл | Що це |
|---|---|
| `index.html` | Уся гра. Канва 320×568, свій ігровий цикл, Web Audio, Firestore через REST |
| `about.html` | Сторінка-кейс: що це, що всередині, числа, як зроблено |
| `signs.html` | 22 таблички A4 для друку, тексти редагуються прямо в браузері |
| `kadr.html` | Сторінка одноразових камер: 19 завдань, у кожного свій код |
| `editor.html` | «Редакторська» — Катя і Діма правили тексти й малювали побажання |
| `scenar.html` | Сценарій гри, візуальна версія для вичитки |
| `dev.html` | Вхід у DEV-режим |
| `stream.html`, `zbir.html` | Трансляція і збір на ЗСУ (посилання вписуються в DEV) |
| `info.html` | Стара внутрішня сторінка «усе про наш сайт» |
| `art/` | 17 PNG: заголовки розділів, написи, дата |
| `audio/` | 18 m4a: 8-бітні та «цифрові» версії треків |
| `CNAME` | лютевесілля.укр (GitHub Pages) |

## Як влаштований index.html

- **Ігровий цикл на canvas.** `game.state` (`title` / `play` / `novel`), усередині
  `game.phase` — конкретна сцена. Близько 35 сцен і мінігор, кожна зі своєю
  `update*()` і `draw*()`. Перемикання — `setPhase('ім'я')`.
- **Дві точки зору.** `game.pov` = `katya` | `dima`. Та сама історія, різні репліки.
- **Сценарії сцен** — функції `*Script(pov)`, повертають масив реплік
  `{who, emo, text}`. Тексти правляться там.
- **Камео гостей** — `guestCameo(sceneKey)`: у сцени підставляється ім'я гостя
  і римований рядок саме про нього.
- **Семплер** — 8 нот × 16 кроків, синтез у Web Audio, можна записати свій звук
  мікрофоном і заспівати поверх біту. Треки лягають у Firestore.
- **Тіндер** — свайп карток гостей із їхніми фото, взаємний лайк = метч.
- **PUBLIC_MODE = true** (константа вгорі) — після весілля: ворота з паролем
  не показуються, `isAuthed()` завжди true, журнал відвідин не ведеться.
  Катя заходить у DEV через `?p=k1tana` або /dev.
- **Список гостей** — `RSVP_GUEST_LIST`, рядки `id;ім'я;сторона;+1;цензура`.
  Паролі гостей прибрано у вересні 2026, лишились самі імена (камео, титри, тіндер).

### Що НЕ показувати публіці

У файлі лежить режим **«Історія без ігор»** (`startNovel()`) — та сама історія,
подана як Game Boy-новела з дизерними кадрами. **Катя його не випускала гостям.**
На нього не веде жодна кнопка: викликати можна лише з консолі або через
`/editor?tab=novel`. Не використовуй його для скріншотів, відео чи демо.
Якщо Катя попросить — його треба або прибрати з публічної збірки, або сховати за DEV.

## Дані (Firebase, проєкт `wedding-6e47f`)

REST без SDK, ключ лежить у коді (він публічний за задумом Firebase).

| Колекція | Що всередині | Доступ |
|---|---|---|
| `selfies` | фото гостей для тіндера й заставки (37) | читання відкрите |
| `matches` | лайки в тіндері (246) | відкрите |
| `sampler-tracks` | мелодії з семплера (15) | відкрите |
| `voice-tracks` | пісні з голосом + журнали: visit, quiz, song, kadr, note | відкрите |
| `memories`, `scores` | спогади, бали | відкрите |
| `emails`, `rsvps`, `sampler-samples` | пошта й відповіді гостей | **закрито (403)** |

У плейлисті працює правило: якщо автор записав пісню **зі словами**, його
інструменталка ховається; однакові мелодії одного автора показуються раз.
З бази нічого не видаляється — фільтр лише на показі.

## Зовнішнє

- Домен: **лютевесілля.укр** (punycode `xn--b1afbvda4be1ira8e.xn--j1amh`)
- Пошта гостям: EmailJS, сервіс `service_hf5mf9r`, шаблон `template_7vvfmfb`
- Збір на ЗСУ: банка monobank `send.monobank.ua/jar/5TEeFknTmt`
- Таблиця гостей: Google Sheets «weddingsmall», вкладка «Гості»
  (гра її не читає — це окремий облік Каті)

## Стан на 21 вересня 2026

- ⚠️ **5 комітів не запушено.** Сайт показує стару версію: ще просить пароль,
  стара сторінка /about, стара локація. Перше, що треба — попросити Катю
  зробити `cd ~/wedding-game && git push` і перевірити лютевесілля.укр/about.
- Зроблено, але не на сайті: публічний режим, паролі гостей прибрано,
  фікс пробілу в полях вводу, сторінка-кейс, локація Ботсад Фоміна,
  семплер без обіцянки призу, порядок у плейлисті.

## Що варто зробити далі

- Вирішити долю режиму «Історія без ігор» (прибрати або сховати)
- Перевірити, чи не лишилось у текстах обіцянок і дат, які вже неактуальні
- Тіндер і семплер живі — вони працюють і зараз, за ними можна стежити
